# Транскрибация аудио через OpenAI Whisper + Flask

Веб-приложение для преобразования аудио/видео в текст.

---

## 📋 Требования

- **Docker Desktop** (скачать: https://www.docker.com/get-started)
- Порт **5003** должен быть свободен

---

## 🚀 Как запустить проект на компьютере

### Шаг 1: Откройте папку проекта
Перейдите в папку `transcriB` на вашем компьютере.

### Шаг 2: Запустите через Docker
Откройте терминал (PowerShell или CMD) в этой папке и выполните:

```bash
docker-compose up --build
```

**Важно:** Первый запуск займет 5-10 минут (скачивается модель Whisper ~500 МБ).

### Шаг 3: Откройте в браузере
Когда увидите сообщение "Running on...", откройте:
```
http://localhost:5003
```

### Шаг 4: Готово!
Загрузите аудиофайл, нажмите "Распознать" и получите текст.

---

## 💾 Как перенести проект на другой компьютер

### Способ 1: Через флешку (проще всего)

#### На старом компьютере:
1. Скопируйте **всю папку** `transcriB` на флешку
2. Убедитесь, что скопировались все файлы (app.py, Dockerfile, templates и т.д.)

#### На новом компьютере:
1. Скопируйте папку `transcriB` с флешки на новый компьютер (например, на Рабочий стол)
2. Установите **Docker Desktop** (если еще не установлен)
3. Откройте терминал в папке `transcriB`
4. Выполните команду:
   ```bash
   docker-compose up --build
   ```
5. Откройте http://localhost:5003 в браузере

**Готово!** Всё работает так же, как на старом компьютере.

---

### Способ 2: Через GitHub (для постоянной синхронизации)

#### На старом компьютере (если еще не загружено):
1. Откройте **GitHub Desktop**
2. Выберите репозиторий `transcriB`
3. Нажмите кнопку **"Publish repository"** (если еще не опубликовано)
4. Выберите название и нажмите "Publish"

#### На новом компьютере:
1. Установите **GitHub Desktop** (https://desktop.github.com/)
2. Войдите в свой аккаунт GitHub
3. Нажмите **File → Clone repository**
4. Выберите репозиторий `transcriB` и нажмите **Clone**
5. Откройте терминал в скопированной папке `transcriB`
6. Выполните:
   ```bash
   docker-compose up --build
   ```
7. Откройте http://localhost:5003 в браузере

**Готово!** Теперь вы можете обновлять проект командой `git pull` в GitHub Desktop.

---

## ⚙️ Дополнительно

### Ускорение на видеокарте (GPU)
Если у вас есть видеокарта NVIDIA:
1. Установите **NVIDIA Container Toolkit**
2. В файле `docker-compose.yml` раскомментируйте секцию с GPU (см. комментарии в файле)
3. Перезапустите: `docker-compose up --build`

Обработка будет в 10-50 раз быстрее!

---

## ❓ Проблемы?

- **Порт занят?** Измените порт в `docker-compose.yml` (строка `"5003:5003"` → `"5004:5003"`)
- **Docker не запускается?** Убедитесь, что Docker Desktop запущен
- **Ошибка при сборке?** Проверьте, что все файлы на месте (app.py, Dockerfile, requirements.txt)

---

## 📁 Структура проекта

```
transcriB/
├── app.py               # Основной сервер
├── templates/
│   └── index.html       # Веб-интерфейс
├── uploads/             # Временные файлы
├── requirements.txt     # Зависимости Python
├── Dockerfile          # Настройка Docker
├── docker-compose.yml  # Простой запуск
└── README.md           # Эта инструкция
```
